/**
 * Saves argument type to temp.input_type
 * @title Set Input Type
 * @category Input Formatting
 * @author Malte Meng 
 * @param {string} input - The input 
 */
const inputTest = async (input) => {
    let re_int = /[0-9]+/
  }
  
  return inputTest(args.input)
  